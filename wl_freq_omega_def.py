
import numpy as np

def wl_freq_omega(mm, stepfreq, startfreq):

    freqcol = np.zeros(mm)
    wlcol = np.zeros(mm)
    omegacol = np.zeros(mm)
    kcol = np.zeros(mm)
    
    return freqcol, wlcol, omegacol, kcol

